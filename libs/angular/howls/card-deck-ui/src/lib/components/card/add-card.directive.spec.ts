import { AddCardDirective } from './add-card.directive';

describe('AddCardDirective', () => {
  it('should create an instance', () => {
    const directive = new AddCardDirective();
    expect(directive).toBeTruthy();
  });
});
